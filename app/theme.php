<?php

/**
 * Reserved include for theme bootstrap.
 *
 * `App\Security\Headers` and `App\Security\SafeLogin` are expected to be
 * constructed from `public/wp-content/mu-plugins/dokpress-bootstrap.php`
 * when that must-use plugin is present.
 */
